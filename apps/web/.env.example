# Local: point the browser at the FastAPI process.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production (Vercel services): leave unset / empty so the web app calls same-origin /api/*.
# NEXT_PUBLIC_API_URL=
