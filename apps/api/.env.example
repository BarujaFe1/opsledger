# Preferências locais (opcional)
# DATABASE_URL=sqlite:///./opsledger.db
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Em Vercel, SQLite usa /tmp automaticamente quando VERCEL=1.
# CORS inclui opsledger-app.vercel.app por padrão se CORS_ORIGINS estiver vazio.
